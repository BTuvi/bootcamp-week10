apiVersion: v1
kind: Service
metadata:
    name: "bootcampmaster-f3fc"
    labels:
        app: "bootcampmaster-f3fc"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "bootcampmaster-f3fc"